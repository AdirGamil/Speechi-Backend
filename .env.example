# ===========================================
# Speechi Backend Environment Variables
# ===========================================
# Copy this file to .env and fill in values
# NEVER commit .env with real credentials

# ---- Environment ----
# Values: development, production
APP_ENV=development

# ---- Server ----
APP_HOST=0.0.0.0
APP_PORT=8000

# ---- API Routing ----
# API prefix for routes
# Development: /api (routes at /api/process-meeting)
# Production: "" or "/" (routes at /process-meeting)
API_PREFIX=/api

# ---- CORS (REQUIRED for production frontend) ----
# Comma-separated list of allowed origins (NO wildcards in production).
# If your frontend is at https://speechi.adirg.dev you MUST set this on the
# API server (speechi-api.adirg.dev) or browser will block requests (CORS error).
# Development: http://localhost:5173,http://127.0.0.1:5173
# Production: https://speechi.adirg.dev
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# ---- MongoDB ----
# Connection URI (includes credentials)
# Format: mongodb://user:password@host:port/?options
MONGODB_URI=
MONGODB_DB_NAME=speechi

# ---- JWT Authentication ----
# Secret key for JWT signing (use a long random string in production)
# Generate with: openssl rand -hex 32
JWT_SECRET_KEY=CHANGE_ME_IN_PRODUCTION_USE_LONG_RANDOM_STRING
JWT_ALGORITHM=HS256
JWT_EXPIRE_DAYS=7

# ---- Usage Limits ----
GUEST_DAILY_LIMIT=1
REGISTERED_DAILY_LIMIT=5

# ---- API Keys (REQUIRED) ----
# OpenAI (Whisper API)
OPENAI_API_KEY=

# Anthropic (Claude)
ANTHROPIC_API_KEY=

# ---- Optional ----
# Custom base URLs (leave empty for defaults)
# OPENAI_BASE_URL=
# ANTHROPIC_BASE_URL=

# Windows only: folder containing Pango/GObject DLLs for WeasyPrint (PDF export).
# Example after installing MSYS2 and mingw-w64-x86_64-pango:
# WEASYPRINT_DLL_DIRECTORIES=C:\msys64\mingw64\bin
# WEASYPRINT_DLL_DIRECTORIES=

# ===========================================
# PRODUCTION EXAMPLE (DO NOT COMMIT)
# ===========================================
# APP_ENV=production
# API_PREFIX=
# CORS_ORIGINS=https://speechi.adirg.dev
# MONGODB_URI=mongodb://user:pass@host:port/
# JWT_SECRET_KEY=your-very-long-random-secret-key-here
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
